# Used by `pnpm test:local` (`vitest run --mode native`).
# Vite disallows `--mode local` (reserved `.local` env file suffix). This file is the mode-specific layer.
# Put machine-specific paths in `.env.local` (always loaded before `.env.[mode]`).

VITE_ENV=local
